namespace Domain {
    public enum SettingType {
        AccountName,
        PoeSessionId,
        ConfigVersion,
        LastUpdateCheck,
        ShowElapsedTime,
        ShowCharName,
        ShowCharXp,
        ShowCharLevel,
        DiscordPipe,
        PersistentTimer
    }
}